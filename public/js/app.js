// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    var app;
    app = window;
    app.AppView = Backbone.View.extend({
      el: $('body'),
      initialize: function() {
        _.bindAll(this);
        return app.chatView = this.chatView = new app.ChatView;
      },
      renderPosts: function() {
        return this.collection.each(function(model) {
          var view;
          view = new app.BlogPostView({
            model: model
          });
          return $('#blogPostBlock').append(view.render().el);
        });
      },
      renderSinglePost: function(slug) {
        var model, view;
        if (this.collection.length === 0) {
          this.collection.fetch({
            async: false
          });
        }
        model = this.collection.getPostBySlug(slug);
        view = new app.BlogPostView({
          model: model
        });
        return $('#singlePost').html(view.render().el);
      }
    });
    return app.app = new app.AppView();
  });

}).call(this);
